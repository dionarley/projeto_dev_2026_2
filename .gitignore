# Banco local — fora do Docker o Django também usa SQLite (raiz do repositório)
data.sqlite
*.sqlite
*.sqlite-*

# Python / Django
__pycache__/
*.py[cod]
backend/.venv/
backend/staticfiles/
backend/.env

# Segredos locais — apenas os .env.example (modelos) são versionados
.env
.env.*
!.env.example

# Logs
logs/
*.log

# Backups de banco (gerados por scripts/db-backup.sh)
db/backups/
